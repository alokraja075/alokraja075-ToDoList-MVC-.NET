services:
  db:
    image: mcr.microsoft.com/mssql/server:2019-latest
    container_name: todolist_db
    environment:
      SA_PASSWORD: "<NEW_REAL_PASSWORD>"
      ACCEPT_EULA: "Y"
    ports:
      - "1433:1433"
    healthcheck:
      test: ["CMD-SHELL", "/opt/mssql-tools18/bin/sqlcmd -S localhost -U sa -P '<NEW_REAL_PASSWORD>' -C -Q 'SELECT 1' || exit 1"]
      interval: 15s
      retries: 10
      start_period: 30s
      timeout: 10s

  web:
    build: .
    container_name: todolist_web
    ports:
      - "8080:80"
    depends_on:
      db:
        condition: service_healthy
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_URLS=http://+:80
      - ConnectionStrings__DefaultConnection=Server=db;Database=ToDo_db;User=sa;Password='<NEW_REAL_PASSWORD>';MultipleActiveResultSets=true;TrustServerCertificate=True
